import 'reflect-metadata';

// zone.js is provided by the root orchestration shell to avoid "Zone already loaded" errors.
// If running standalone, the index.html template includes it via CDN.
// import 'zone.js/dist/zone'; 
